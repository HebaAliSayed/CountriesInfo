<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidPluralsightCourse" parent="ThemeOverlay.MaterialComponents.Dark">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
    </style>


    <style name="Theme.AndroidPluralsightCourse.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AndroidPluralsightCourse.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AndroidPluralsightCourse.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.AndroidPluralsightCourse.NoActionBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AndroidPluralsightCourse.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AndroidPluralsightCourse.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CourseTitle" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/teal_700</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>